# REVISIONS — master index to follow every change & the progress
<!-- Author directive 2026-07-28: apply the revision-number rule to ALL files so
     the changes and progress are followable. THIS file is the single place to
     track it. Rule: every deliverable carries _R<n>; the first version is R1;
     any change = a NEW revision file (never edit a working file in place).
     Author directive 2026-08-03: MINOR change = _R<n>.<m> (e.g. R4.1, R4.2); MAJOR change = _R<n+1>.
     git commits are the dated timeline (bottom). -->

## How to read this
- **Current** = the revision you should use now. Older revisions are kept (frozen) for provenance.
- When something changes, a new `_R<n>` file appears here with a one-line note.
- One-off snapshots (a review transcription, an audit) are dated records, not evolving deliverables — listed under §6.

---

## 1. Solver (MATLAB)
<!-- 2026-08-28 RENAME (author request): the whole solver family's filename prefix
     changed claude_R# -> LSTE_solver_R# (Lord-Shulman ThermoElastic), across ALL
     9 revisions (R1..R9), the 2 R9 companion scripts, the 4 R2/R3 validation
     drivers, and the regression test (test_r7_regression.m -> test_LSTE_solver_R7_regression.m).
     Pure rename (via git mv, history preserved) -- NO content/physics change.
     Every literal internal/external reference to the old "claude_R" identifier
     was updated in the same pass: the 2 live campaign scripts that actually
     invoke the solver (run_ch4_campaign.ps1 calls LSTE_solver_R7, run_nz_sweep.ps1
     calls LSTE_solver_R7, run_conv_explore.ps1 calls LSTE_solver_R9), archived
     campaign scripts, all validation/regression drivers' internal calls, and
     every prose mention across code_docs/*.md, governance/*.md, README.md (both),
     and this file. Verified with a smoke test after the rename: LSTE_solver_R9
     and LSTE_solver_R7 both run a small config cleanly, and the full R6-vs-R7
     regression test (test_LSTE_solver_R7_regression.m) reports DIGIT-IDENTICAL
     physics + all DOF-map checks pass, same as before the rename. Does NOT touch
     the already-computed param_studies_ch4/*.mat results (they don't reference
     filenames internally) or any solver's physics/content. -->
| Artifact | Current | History |
|---|---|---|
| `LSTE_solver_R#.m` (main solver) | **R9** (performance) — **R8 is still the thesis-of-record revision**, kept frozen | R1 fixed the broken dynamic solver (frozen) → R2 cfg engine + full-history + sine pressure → R2_1 audit fix → R3 BC options → R3_1 final porosity → R4 Gaussian shock → R5 theory switch (Fourier/LS/DPL/GN3) → R6 per-end BC + cfg-overridable GPL dims → **R7 explicit DOF numbering/mapping matrix (NodeMap + DOFmap + GridDOF + CSV export); physics digit-identical to R6 (Prom.2 p.4) → **R8 FINAL/DEFINITIVE revision — physics byte-identical to R7; default config set to the thesis reference case (LS, N_L=7, R_i=1.0, R_o=1.5, L=2.1, N_r=15, N_z=11, W=0.3%, h_c=10, 300→600 K, P_i=50 MPa, tau0=418 s, 3000 s / dt=1) so a standalone run reproduces the thesis; porosity patterns left for the author to finalize per MZ (A7)** → **R9 (2026-08-06, performance-only) — vectorized the interior assembly loop (section 4.1): every scalar sparse-matrix write inside the per-node `for jr`/`for jz` loops replaced with one batched row-vector write (using `A_r{e}(ir,:)`/`A_z(iz,:)` directly) or, for the double-loop u/w elastic-coupling blocks, one `kron()`-built write over the contiguous per-layer column range; row equilibration also reworked to avoid forming a dense `Ndof × 3·Ndof` matrix. Section 4.2 (every boundary/interface condition) is byte-for-byte copied from R8, untouched. Validated: R8-vs-R9 diffed field-by-field across 5 configs (default/FOURIER/DPL/GN3/different mesh) and the full 3000-step thesis reference case — every field matches to floating-point roundoff (relative ~1e-9 to 1e-16); independently re-checked against literature benchmark 1 (Malekzadeh & Heydarpour), same documented accuracy as always. Timing (sequential, uncontended): R8 1039.1 s (899.9 s assembly+setup) vs R9 495.2 s (364.4 s assembly+setup) on the full reference case — **≈2.1× faster overall, ≈2.5× faster on assembly**. Not yet adopted as the thesis-of-record revision — use R8 for anything that must match already-published numbers; R9 is available for anyone who needs faster iteration.** |

## 2. Parametric-campaign figure scripts
| Artifact | Current | History |
|---|---|---|
| `claude_param_figures*.m` | **ARCHIVED** (old geometry) | base → R2 → R3 → R4 (+`_R4_color`). OLD-geometry Ch4 figure lineage that read the now-deleted `param_studies\`; **superseded in practice by the `claude_catalog_R6*` family** (§3, which reads `param_studies_ch4\` → `figures_ch4_*`). Moved to `code/_archive/` in the 2026-08-05 code audit (Prom 5 #6). |

<!-- 2026-08-05 CODE AUDIT / REORG (Prom 5 #6): current solver = LSTE_solver_R8 (physics ≡ R7; the Ch4 campaign still invokes R7). Current campaign = campaign/run_ch4_campaign.ps1 + run_nz_sweep.ps1 → param_studies_ch4 (86 cases). Current Ch4 figures = claude_catalog_R6* family. Current stats = code/misc/claude_chapter_stats_ch4.m → chapter_stats.csv. ARCHIVED to code/_archive/ (17 files): param_figures lineage, catalog_R1..R5(+color), old claude_chapter_stats.m, test_r5_hooke.m, claude_T2_spatial_methods.m (superseded by T2_1). ARCHIVED to campaign/_archive/ (7 files): run_param_studies v1..v6, run_R6_verify.ps1. The old param_studies/ folder (old-geometry NL=5 runs) was deleted. Both code_docs updated to R8/current pipeline; matrix documented as epoxy. -->

### 2a. Methodological extension studies (T1/T2/T3, `code/extensions/`, output → `results_extensions/`)
| Artifact | Current | History |
|---|---|---|
| `claude_T2*_spatial_methods.m` (spatial-discretization comparison: DQM vs FDM vs FEM) | **T2.9** | T2 (archived, superseded) → **T2_1** = added FEM (linear + quadratic Galerkin) alongside DQM/FDM, kept T2 frozen → **T2.2 (2026-08-22, minor)** = extended the DQM node sweep from `[7 9 11 15 21 31]` to `[7 9 11 15 21 31 41 61 81 121 161 201]` (author request: DQM curves stopped around N≈20–31 while FDM/FEM ran out to N=321, making the comparison look incomplete). Finding: **Chebyshev-node DQM stays well-conditioned and plateaus at the same temporal-error floor (~1.1e-3 K) as FDM/FEM all the way through N=121**, then fails (NaN) at N≥161 — evidence it converges over a comparable range, just like FEM. **Uniform-node DQM diverges catastrophically beyond N=31** (error ~1e47–1e52 at N=41/61, then NaN) — classic Runge-phenomenon ill-conditioning of a global polynomial basis on equally spaced nodes; annotated directly on the figure rather than left unexplained. y-axis capped at `[1e-4, 3e-1]` so the divergent uniform-DQM branch doesn't flatten the readable curves. `T2_1` kept frozen/untouched — the fix was made in a new `T2_2` copy per the revision-number rule. → **T2.3 (2026-08-24, minor, styling only)** = legend moved `southwest`→`northeast`; removed the "DQM (uniform) diverges for N≥41" text annotation (author request — also brings this figure in line with the project's no-floating-text plotting rule, §7, which this script had missed). No numeric change. `T2_2` kept frozen/untouched. → **T2.4 (2026-08-24, minor, styling only)** = line colors switched back to the "first edition" palette (blue/red/yellow/purple/green, MATLAB's default color order) that the original archived `T2` figure used, replacing T2_1–T2_3's blue/magenta/red/green/black scheme; slope-2 reference line solid→dotted; markers changed to hollow (no fill) instead of solid-filled (author requests). No numeric change. `T2_3` kept frozen/untouched. → **T2.5 (2026-08-24, minor, styling only)** = connecting lines removed from the data series — markers only (slope-2 dotted reference line kept) (author request). No numeric change. `T2_4` kept frozen/untouched. → **T2.6 (2026-08-24, minor, styling only)** = connecting lines restored between markers (author wants first-edition look: colored lines + hollow markers together). No numeric change. `T2_5` kept frozen/untouched. → **T2.7 (2026-08-24, minor, styling only)** = markers switched to white-face (author request: match the same display standard as the other Chapter 4 figures, e.g. `claude_catalog_bestworst.m` — colored solid line + colored marker edge + white fill, so the line is masked inside each marker glyph but stays visible between markers). No numeric change. `T2_6` kept frozen/untouched. → **T2.8 (2026-08-26, minor)** = author request: (1) x-axis label dropped the "N" (`'number of radial points N'` → `'number of radial points'`); (2) DQM(Chebyshev) sweep densified between the last good point and first failure — added N=131/141/151 to `[... 121 161 201]` — pinpointing the breakdown to **between N=141 (still valid) and N=151 (NaN)**, tighter than the previously known "121–161" range; (3) FEM(quadratic) sweep extended from `[11 21 41 81 161]` to include N=321, matching FDM/FEM(linear)'s range — confirms FEM(quad) has already hit the fixed dt=0.01 temporal-error floor by N=161 (err at N=321 ≈ err at N=161). `T2_7` kept frozen/untouched. → **T2.9 (2026-08-26, minor)** = author decided the T2.8 breakdown-pinpointing sweep wasn't wanted for the shipped figure — reverted the DQM/FEM(quad) node arrays back to T2.7's (`[7 9 11 15 21 31 41 61 81 121 161 201]` / `[11 21 41 81 161]`), keeping only the "N" dropped from the x-axis label. `T2_8` kept frozen/untouched (its breakdown-window finding, N=141 valid / N=151 NaN, stays documented above). |
| `claude_T4*_axial_methods.m` (AXIAL-direction spatial-method comparison: DQM vs FDM vs FEM) | **T4.3** | New (2026-08-25, author request): the axial companion to T2 (which is radial-only — its Bessel benchmark has no z-dependence, so it can't just be relabelled). Own separate 1-D **axial** benchmark: a finite rod on [0,L], `rho c θ_t = k θ_zz`, Dirichlet ramp at z=0 + insulated at z=L, with an exact Fourier-series solution (eigenfunctions sin(bₙz), bₙ=(2n-1)π/2L). Same five methods, same display standard as T2.7 (first-edition palette, white-face markers, dotted slope-2, top-right legend); y-floor lowered to 1e-5 (this axial problem resolves ~1 decade finer than radial, so T2's 1e-4 floor would clip the DQM/quadratic-FEM data). **Finding mirrors the radial T2 result**: DQM-Chebyshev and quadratic-FEM are the most efficient per node (reach ~2–5e-5 by N≈11–21); uniform-node DQM Runge-diverges past N≈31 (~1e50); FDM and linear-FEM show clean 2nd-order (slope-2) convergence and need ~15× more nodes than DQM/quad-FEM to match — consistent with the thesis's "DQM ≈15× fewer nodes than FDM/linear-FEM" claim in both directions. Output → `results_extensions/figures/T4_axial_convergence.png/.fig` + `T4_axial_table.csv`. → **T4.1 (2026-08-25, minor, figure only)** = y-axis floor lowered 1e-5→1e-6 so the lowest DQM(uniform) point (N=31, err≈7.5e-6) is no longer clipped off the bottom (author request). No numeric change. `T4` kept frozen/untouched. → **T4.2 (2026-08-26, minor)** = same three changes as T2.8 (mirrored for consistency): x-axis label drops "N"; DQM(Chebyshev) densified N=131/141/151, same breakdown window confirmed (**valid through N=141, NaN at N=151**); FEM(quadratic) extended to N=321 (already at its dt=0.01 error floor by N=161, same as T2.8). `T4_1` kept frozen/untouched. → **T4.3 (2026-08-26, minor)** = mirrors T2.9 — reverted T4.2's densified/extended node arrays back to T4.1's, keeping only the "N" dropped from the x-axis label. `T4_2` kept frozen/untouched. |
| `claude_catalog_relax_finer_N35*.m` → `results_extensions/figures_relax_finer/C_relaxation_N35*.png/.fig` | **DONE (2026-08-26, author request, exploratory)** | Part of the negative-T*-dip investigation (2026-08-25/26; see chat — the N_r=15 official campaign shows a small negative T* dip for the LS relaxation-time cases, root-caused as spatial-DQM under-resolution that shrinks substantially with N_r refinement, distinct from the Fourier case's tiny residual dip which is confirmed genuine thermoelastic-coupling physics, not fixable by refinement). Re-renders the official `C_relaxation` 4-panel figure (T*(Fo), U*(Fo), T*(ξ), Σ_θθ(ξ)) from the finer-mesh (N_r=35, N_z=11, total_time=6000, dt=1) diagnostic data in `param_studies_relax_finer\` instead of the locked N_r=15 campaign. `_N35.m` = B&W style (mirrors `claude_catalog_R6.m`), all 4 relaxation cases. `_N35_color.m` = color style (mirrors `claude_catalog_R6_color.m`'s exact palette/legend convention), narrowed to just **Fourier vs LS** (τ*=0.15) per author request, with a light adaptive `smoothdata('movmean')` pass on the raw signal before interpolation to declutter small-scale sample-to-sample noise — the genuine sharp LS wavefront jump near Fo≈0.2 (present in the official N_r=15 figure too) is deliberately left intact, not smoothed away. NOT part of the official campaign/thesis figures — output kept in its own `figures_relax_finer\` folder, never touches `figures_ch4_*`. New files. **Convergence finding (2026-08-26)**: min T* for C_TAU_015 across three mesh densities — **N_r=15: -0.0296 → N_r=35: -0.0124 (-58%) → N_r=55: -0.0112 (-10%)** (single extra data point, `param_studies_relax_finer/C_TAU_015_NR55.mat`, not used in the shipped figure). The dip **shrinks but does not vanish** — diminishing returns from N_r=35→55 vs. the large N_r=15→35 drop suggest it is converging toward a small nonzero residual, i.e. it likely has the same character as the Fourier case's confirmed genuine coupled-physics floor (not purely spatial-DQM under-resolution as first hypothesized), though this has not been separately confirmed with a coupling on/off test at LS mesh densities. **Author decision**: N_r=35 is the deliverable (already-built `C_relaxation_N35*.png`); C_TAU_004/C_TAU_044 were not rerun at N_r=55 (diminishing-returns finding makes it not worth the ~1000s/run cost — the N_r=55 factorization alone took disproportionately longer than the whole N_r=35 run). |


### 2b. Exploratory convergence study (author request 2026-08-24; NOT the locked reference mesh)
| Artifact | Current | Note |
|---|---|---|
| `campaign/run_conv_explore.ps1` + `code/catalog/claude_catalog_conv_explore.m` → `param_studies_conv_explore/` + `figures_conv_explore/` | **DONE** | Standalone diagnostic conv_master figure at a new anchor **N_r=11, N_z=13, N_L=7** (the locked Ch4 reference mesh, N_r=15/N_z=11, is untouched — this does not replace it). 15 fresh solver runs via **LSTE_solver_R9** (validated digit-identical to R8, ~2.1× faster), each varying one of N_r∈{5,7,9,11,13,15}, N_z∈{5,7,9,11,13,15}, N_L∈{3,5,7,9,15} with the other two held at the anchor. Same reference-case physics as the locked campaign (`run_nz_sweep.ps1`). Finding matches the locked-mesh study's conclusion: **N_z is the binding direction** — L₂ hoop-profile error starts >5% at N_z=5, crosses below 1% by N_z=7, while N_r and N_L are already under ~0.4% even at their coarsest tested value. Output kept fully separate from `param_studies_ch4`/`figures_ch4` so the locked campaign is unaffected. |

## 3. Quantity CATALOG (figure-selection tool)
| Artifact | Current | History |
|---|---|---|
| `claude_catalog_R#.m` | **R6** (`_R6` 4-panel · `_R6_full` · `_R6_extras` · `_conv`) | R1 = frozen (one 9-panel image) → R2 = two-part (hist+prof) + makima-smoothed → R3 = adds ε_rr and ε_zz strain profiles to Part 2 (8 panels; ε_zz then from last snapshot) → R4 = weight-fraction zoom study (D2) swapped to low range W = 0.1/0.3/0.5/0.9/1.5% → R5 = ε_zz recomputed from saved final-time stresses via Hooke's law (exact; 2μ per case) → **R6 = full rebuild for the NEW geometry (R_i 1 / R_o 1.5 / l 2.1, mesh N_r 15 / N_z 11) and the app10041397 (Heydarpour) dimensionless convention (Fo=α̂t/h², h=wall); split into `claude_catalog_R6.m` (4-panel selection: T*,u* vs Fo + T*,Σ_θθ vs ξ), `claude_catalog_R6_full.m` (8-panel hist+prof, all strains+stresses, Hooke ε_zz), `claude_catalog_R6_extras.m` (second-sound wave-fronts + 5×5 GPL×porosity matrix) and `claude_catalog_conv.m` (N_r/N_z/N_L/Δt convergence + min-nodes master). Prom.3 applied: 25-case matrix, a/b & b/t legends, sine-pressure study dropped.** |
| `FIGURE_CATALOG_R6_FA.docx` (+ `_R6.md`) | built from catalog **R6** (44 figures; color variant + EN pending author's selections) | on each catalog Rn the deliverables are renamed `_R<n>` too |
| `claude_catalog_matrix25_4panel.m` → `matrix25_4panel.png/.fig` (bw + color) | **DONE (2026-08-25, author request)** | All 25 GPL×porosity cases overlaid in ONE standard Ch4 4-panel figure (T* vs Fo, U* vs Fo, T* vs ξ, Σ_θθ vs ξ) — a complement to the existing 5×5 grid-of-subplots view (`claude_catalog_R6_extras`) and per-porosity unpacking (`claude_catalog_gplpor`). Legibility scheme for 25 curves: **color = GPL pattern (5), marker shape = porosity pattern (5)** (MATLAB has only 4 line styles, so marker-shape realizes "style per porosity"); solid lines, white-face markers (Ch4 standard), compact 10-entry legend (5 GPL + 5 porosity) via dummy handles in panel 1. B&W variant uses 5 gray levels for GPL. Reads the 25 cached `H_<GPL>_<Por>.mat` (no solver runs). Inherently dense (25 curves) — temperature panels read cleanly; U* and layered hoop-stress panels are busy by nature. `claude_catalog_matrix25_4panel.m` kept frozen/untouched. |
| `claude_catalog_matrix25_4figs.m` → `matrix25_T_Fo/U_Fo/T_xi/S_xi.png/.fig` (bw + color) | **DONE (2026-08-25, author request)** | Same 25-case dataset and color=GPL/marker=porosity scheme as `matrix25_4panel`, but split into **4 separate full-size figures** (one per quantity) instead of one 2×2 panel — each is bigger and reads more easily, especially the U* and hoop-stress ones. Not a revision of `matrix25_4panel` (different deliverable, same source data) — both kept. |
| `claude_ch123_figs*.m` → `figures_ch123/*.png` (6 chapter-1/2/3 schematics: geometry, GPL patterns, porosity patterns, DQM nodes, flowchart, theory taxonomy) | **R3** (porosity schematic only) | Edited directly in place through 2026-08-26 (no `_R<n>` suffix, unversioned utility script — same standing as `claude_catalog_bestworst.m`): x-axis label simplified on the porosity schematic to `\xi = (r-R_i)/h` (author request, drops the verbose "through-thickness coordinate ... (ζ = ξ-1/2)" text to match every other Ch4 figure's bare-ξ convention). → **R2 (2026-08-27, author request, explicitly asked for a real revision this time)** = porosity V/A pattern leading coefficient **2 → 1.5** in `Pm.V`/`Pm.A` (lines 125-126) — lowers the V/A curves' peak/trough from ≈2/0 to ≈1.13/0. Author corrected the first pass of this revision (which had overwritten `porosity_patterns_schematic.png` in place) — **output kept alongside, not overwriting**: `porosity_patterns_schematic.png` (coefficient=2, original, untouched in git) and the new `porosity_patterns_schematic_R2.png` (coefficient=1.5) both exist side by side; `claude_ch123_figs_R2.m` writes the porosity figure to the `_R2` filename specifically so future runs never clobber the original. Only the porosity schematic's data changed; the other 5 schematics are unchanged content and still write to their original filenames (unversioned, re-exported in place per this script's established handling). `claude_ch123_figs.m` (pre-R2, i.e. the state through the label fix) kept frozen/untouched. → **R3 (2026-08-27, author request)** = two further changes to `Pm.V`/`Pm.A`: (1) leading coefficient reverted **1.5 → 2**; (2) the first fraction's denominator inside `cos(...)` changed **2 → 4**, i.e. `cos(pi*zet/2 ± pi/4)` → `cos(pi*zet/4 ± pi/4)` — spreads the V/A variation over more of the domain, flattening the curves to a ≈0.3–1.7 range (vs. R2's steeper ≈0/1.13). Same non-destructive pattern as R2: output saved as `porosity_patterns_schematic_R3.png`, R1's original and R2's `_R2.png` both left byte-identical/untouched. `claude_ch123_figs_R2.m` kept frozen. **Not yet propagated**: `claude_ch3_gpl_porosity_stacked.m` duplicates the original (pre-R2/R3) `Pm.V`/`Pm.A` formula inline — will show inconsistent V/A peaks vs. this R3 schematic until synced. |
| `claude_pattern_gradient_schematics.m` → `GPL_pattern_gradient.png` / `porosity_pattern_gradient.png` | **NEW (2026-08-29, author request)** | Alternative to the line/curve schematics (`GPL_patterns_schematic.png`, `porosity_patterns_schematic.png`): shows the 5 GPL and 5 porosity distribution patterns (UD/O/X/V/A) as a **gradient** — a quarter-annulus split into the 7 concentric layer bands, each shaded by its actual intensity (darkest = most graphene / most porous), no curves/axes-plots. Style matches the convention in Rezaei's MSc thesis (same university, same UD/X/O/V pattern family, Fig. 2-3 p.17) and the cited FGM literature, extended to all 5 patterns used in this thesis. Uses the same frozen reference geometry/formulas as `claude_ch123_figs.m` (Ri=1.0, Ro=1.5, NL=7, W=0.3%, em3=0.8980 — the ORIGINAL coefficients, not the R2/R3 porosity experimental variants), evaluated at the 7 layer midpoints rather than the continuous curve. One shared colorbar per figure (GPL: white→dark blue; porosity: white→dark gray) so all 5 patterns are directly comparable. New file; does not touch `claude_ch123_figs*.m`, `claude_ch3_gpl_porosity_stacked.m`, or their PNGs — a companion visualization, not a replacement. |

<!-- 2026-08-06 PLOTTING-RULE SWEEP: author's strict MATLAB rules (no title()/sgtitle()/floating text(), only axis/tick labels + legend; color figures solid lines only, B&W figures keep varied line styles as the color-free differentiator) applied across every active AND archived figure script. claude_catalog_R6.m/_R6_color.m/gplpor.m/bestworst.m were already compliant from an earlier pass. Fixed: claude_ch123_figs.m, claude_catalog_R6_full.m, _R6_extras.m, _ch4_variants.m, _conv.m, the validation/anim/extensions scripts, and all 15 files in code/_archive/ (source-only, not re-run — dead code). Two content-necessary exceptions kept (author-confirmed): the flowchart/taxonomy box labels in claude_ch123_figs.m (the text IS the diagram), and the 25-matrix per-cell identity titles (required by Prom.4's "row/column labels on every subplot"). While fixing this, found and fixed a REAL bug: claude_catalog_R6_full.m/_R6_extras.m/_ch4_variants.m/_conv.m used relative output paths, and MATLAB's run() changes cwd to the script's own folder, so the FIRST attempt at this sweep silently wrote 85+ regenerated figures into code/catalog/figures_ch4* instead of the real claude/figures_ch4* — same class of bug as the chapter_stats_ch4.m fix below. Switched to absolute CLROOT-based paths (matching claude_chapter_stats_ch4.m's established fix), cleaned up the debris, regenerated correctly, reconciled the staging-folder (figures_ch4/) → embedded-folder (figures_ch4_bw/_color) copies that the fix exposed. -->

## 4. Results chapter 4
| Artifact | Current | History |
|---|---|---|
| `RESULTS_CHAPTER_FA/EN*_R#.md` (+ docx) | **R4.4** | base → R2 (authored FA+EN) → R3 (old geometry: split convergence/validation, aspect+infinite, 4×4 matrix, pressure 0–100 MPa, peak-trough/mirror) → **R4 = full rebuild for the NEW geometry (R_i 1 / R_o 1.5 / l 2.1), Heydarpour dimensionless convention (Fo=α̂t/h²), notation l=length/h=thickness; all 21 sections' numbers re-extracted from `param_studies_ch4`; Prom.3 applied (25-case matrix, a/b & b/t legends, sine study dropped); delivered as 4 docx (EN/FA × bw/color, 23 inlined figures each) built from `figures_ch4_bw` / `figures_ch4_color`. Heydarpour citation resolved to [69].** → **R4.1 (minor, Prom.5)** = removed the descriptive top-title from every figure (4-panel study, convergence, benchmarks — axis labels + legends kept, per author's "delete the title on top, keep the legend"); dropped Fig 4-6 (bench3: only the present solution was plotted, no Bagri & Eslami data overlaid → not a real comparison) and renumbered figures to **4-6…4-23** in both EN & FA (also fixed a stale pressure-figure cross-reference, 4-11→4-15); cropped benchmark 1 (Fig 4-4) to t*∈[0,0.5] to zoom the three-point match; benchmark 2 (Fig 4-5) title removed, legend upper-right. 4 docx rebuilt (23 figures each). **R4 kept frozen.** → **R4.2 (minor)** = added a new per-porosity figure set (author request): 5 figures at **4-19…4-23**, one per porosity pattern (UD/O/X/V/A), each overlaying the five GPL patterns in the 4-panel format — the 25-matrix unpacked pattern-by-pattern (matrix kept alongside). Previous 4-19…4-23 (coupling → theories) shifted to **4-24…4-28** in EN & FA. New script `claude_catalog_gplpor.m`; 4 docx rebuilt (28 figures each). **R4.1 kept frozen.** → **R4.3 (minor)** = corrected the length symbol in Fig 4-16 (Q_length): legend label **l → L** (`l=1` → `L=1`) — the last lowercase-l the Prom.4 l→L pass missed in a figure (caption and §4-12 prose already used L). Figure-only fix, text identical to R4.2; Q_length re-rendered (B&W + colour), 4 docx rebuilt. **R4.2 kept frozen.** → **R4.4 (Prom 5 review; in progress)** — applies the supervisor's **Prom 5** review (proms numbered sequentially: this is the 5th). **Item 1 done**: a detailed explanation of the sharp spike in the T*(ξ) profiles — the Lord–Shulman second-sound wave front caught in the outer layers at the final time (trough on the fifth interface ξ = 5/7 ≈ 0.71, crest just past it near ξ ≈ 0.75), with the minor serrations at the other interfaces ξ = k/7 — added to §4-5, EN + FA (28 figures unchanged). **Items 2 & 3 done**: item 2 = a best/worst 4-panel figure (**Fig 4-24**, §4-13: V-A best thermal / A-UD worst thermal / X-V worst hoop stress, vs the UD-UD reference), extremes computed from the saved 25-case data (new script `claude_catalog_bestworst.m`); item 3 = R3's Fig 4-2 (`spatial_convergence`, top title removed) added to §4-19 as **Fig 4-29** with an explicit slope-2 definition (second-order convergence, error ∝ N⁻²). Coupling→theories refs shifted to **4-25…4-30**; 4 docx rebuilt (**30 figures each**). **Items 4–9 (thesis-level) also done** since: master figure file → §5 (Master TOC/Figure List/Flipbook); critical review → `governance/EXAMINER_REVIEW_FA.md`; code-doc update → §5; improvement list → `governance/IMPROVEMENT_SUGGESTIONS_FA.md`; English Background → `thesis_chapter/BACKGROUND_EN.md`; defense Q&A → `governance/DEFENSE_QA_FA.md` (37 Q&A pairs; a `\rm`-in-math rendering bug in 5 formulas fixed 2026-08-06 — see §7). **R4.4 is COMPLETE. R4.3 and earlier kept frozen.** All 4 R4.4 docx (EN/FA × bw/color) also picked up the project-wide plotting-rule sweep (§7, 2026-08-06): no figure titles/floating text, solid lines on every color figure (25-matrix per-cell identity titles and the flowchart/taxonomy box labels kept as deliberate content exceptions). Open: final animation (author-blocked, needs view/field/case/format choice); §4-17 wave-front optional. |
| `FIGURE_CAPTIONS*.md` | **R3** | base → R2 → **R3** (22 figs, new numbering) |

## 5. Other thesis deliverables (updated 2026-08-06 — this table was badly stale, see below)
| Artifact | Current | Note |
|---|---|---|
| `CHAPTER1_framework_FA_R1` / `CHAPTER2_litreview_FA_R1` / `CHAPTER3_formulation_FA_R1` (+ docx) | **DONE (R1)** | drafted, citation-cleaned; Ch3 Table 3-1 matrix corrected epoxy (was mislabeled metal); Ch3's 5 schematic figures embedded 2026-08-05 (geometry, GPL patterns, porosity, DQM nodes, flowchart) — porosity schematic (Fig 3-3) re-plotted 2026-08-06 to show the actual porosity coefficient e=P_m² per thesis eq. 3-39, not the mass factor P_m it originally (wrongly) plotted; DQM-nodes schematic uses N=9 illustrative points, no titles/floating text (author's plotting rules, §7). Colour track-changes versions exist for all three; **`_R1_tracked` is frozen/byte-identical to its original state — do not edit.** Two bugs were found and fixed 2026-08-22 in a new **`_R1.1_tracked`** revision (not in place — first attempt at this wrongly edited `_R1_tracked` directly, caught by the author's revision-number rule and corrected by reverting `_R1_tracked` from git history and moving the fix to `_R1.1_tracked`): (1) the tracked `.docx` files never actually showed color — pandoc silently drops raw-HTML `<span style="color:...">` on docx output, so every "addition" rendered as plain black text; `_R1.1_tracked` uses pandoc's native `[text]{custom-style="TrackAdd"/"TrackDel"}` span syntax with real `TrackAdd`/`TrackDel` character styles (green / red+strike) in the reference-doc — verified color survives in the actual docx XML. (2) all 3 tracked docs had drifted from their current final chapters (found while investigating #1): Ch1 was missing a citation, Ch2 had two swapped reference numbers ([5]/[6]/[78] mixed up), Ch3's tracked copy predated the material-properties table (3-1), the γ=1/2/percolation sentence, and the 5 embedded figures (still had `[شکل: …]` placeholders) — all resynced to match the current final chapters in `_R1.1_tracked`. |
| `ABSTRACT_FA` / `ABSTRACT_EN` (+ docx) | **FA: R1 · EN: R2** | aligned to Results Chapter R4.4's real numbers; **2026-08-24 (author request)**: Findings/Conclusion paragraphs shortened and summarized (Findings from ~16 lines to 4 sentences keeping only the headline numbers — overshoot 1.115, V-A synergy 26×, Gaussian shock 2.5×, DQM/Newmark efficiency; Conclusion from ~12 lines to 3 sentences) — same content in FA and EN. **2026-08-25**: restored the "vs finite-difference and *linear* FEM" qualifier on the ~15×-fewer-nodes claim (the 2026-08-24 shortening had over-compressed it to "competing methods", which wrongly implied DQM beats quadratic FEM too — the T2 benchmark shows quadratic FEM is comparably efficient, sometimes better; Chapter 5 §5-2 always had the correct qualifier, only the abstract had dropped it). → **ABSTRACT_EN_R2 (2026-08-28, author request)** = replaced with a direct English translation of the author's newer Persian abstract draft from a separate Google Drive document ("پیش نویس پایان نامه (R 1.1).docx") — continuous-paragraph structure (no Findings/Conclusion split), less numeric detail than R1 by the author's deliberate choice (drops the 1.115 overshoot figure, the 26× and ~1/15th-nodes figures, and explicit V/A pattern naming — author confirmed this is intentional, not an error). `ABSTRACT_FA` stays at R1 (untouched — only the English translation was requested); `ABSTRACT_EN.md` (the R1 content) kept frozen/untouched. |
| `APPENDIX_A_methods_FA/EN` (+ docx) | **DONE (R1)** | Newmark + comparison methods (Prom.2 و) |
| `APPENDIX_B_validation_FA` (+ docx) | **FA: R1 · R2 added** | 6 validation figures embedded from `figures_ch4_bw`; ANSYS framing corrected (own-reported column, not an independent solve). → **APPENDIX_B_validation_FA_R2 / _EN_R2 (2026-08-28, author request)** = plain-language rewrite of the same 5 validation tests + convergence evidence (same quantitative results: static-assembly diff ~2e-11, benchmark-2 ~0.1-0.2%, Bessel-series ~1e-5 to 1e-7, ode15s diff 0.003 K, Bagri-Eslami wave fronts r=1+t / r=1+0.5t, Nr/Nz convergence, ~15x-fewer-nodes claim) — filling the gap found when the author's newer Google-Drive thesis draft turned out to be missing Appendix B entirely. `_EN_R2` is the FIRST English version of Appendix B (no prior English existed). `APPENDIX_B_validation_FA` (R1) kept frozen/untouched. |
| `PROPOSED_ADDITIONS_FA` (+ docx) | **NEW (2026-08-28)** | Collects 5 other plain-Persian proposed text additions (2 Ch1 limitations + a geometry sentence, Ch2's closing §2-7 synthesis, Ch3's closing chapter summary, Ch4's dimensionless-quantities intro for §4-2, 1 Ch5 limitation + 2 future-work items) into one file for author review — not yet inserted into any chapter file. Drafted to reconcile gaps found comparing the author's newer Google-Drive draft against the fuller repo chapters, in the author's requested plain register. |
| `CHAPTER5_conclusions_FA/EN` (+ docx) | **DONE (R1), FA and EN now in numeric agreement** | conclusions + §5-4 "Limitations of the study" (2026-08-21, both languages, future-work renumbered to §5-5). Found 2026-08-21 that `CHAPTER5_conclusions_EN.md`'s §5-2 findings were still the old pre-R4.4 numbers (overshoot 1.41/1.78, GPL-V −16%/5.5×, 4×4 matrix, 9×/50× headline figures) despite an earlier row claiming both languages were refreshed. Fixed 2026-08-22: EN §5-2/5-3 rewritten number-for-number to match FA/R4.4 (overshoot 1.109/1.115, GPL-V −29%/3×, porosity-A 8×, synergy 26-fold, 5×5 matrix, coupling damping 1%, clamped-support displacement −9%, Gaussian shock 2.5×); docx regenerated. `DECISIONS_NEEDED.md` item 10 closed. **2026-08-24 (author request)**: §5-5 future-work list extended with two new items (8, 9) — experimental validation (fabricate/test physical specimens under real thermal shock, measure τ₀ directly) and applied design guidance (engineering selection chart from the V-A synergy finding, industrial-geometry case study) — both languages, docx regenerated. |
| `MASTER_TOC_FA` (+ docx) | **DONE (R1)** | anchors fixed (stripped ZWNJ/en-dash so GitHub-slug links resolve); front-matter changed from a raw `<div dir=rtl>` wrapper to YAML `dir:`/`lang:` (2026-08-05) so pandoc renders it RTL correctly |
| `MASTER_FIGURE_LIST_FA` (+ docx) | **DONE (R1)** | all 42 numbered figures; Fig 3-3 row updated for the porosity-coefficient replot (2026-08-06) |
| `MASTER_FIGURES_FLIPBOOK_FA` (+ docx) | **DONE (R1)** | 36 figures (Ch2:1, Ch3:5, Ch4:30) inlined |
| `CODE_DOC_usage_FA` / `CODE_DOC_technical_FA` (+ docx) | **DONE, current as of R8/R9** | fully corrected 2026-08-05 (was documenting R7 defaults/a false runtime-warning claim/dead campaign scripts): now documents R8's thesis-reference defaults, the real `run_ch4_campaign.ps1`+`catalog_R6*` pipeline, the epoxy matrix. **Not yet updated to mention R9** (§1) — see open items below. |
| `CODE_FIX_HISTORY_EN/FA` (+ docx) | **NEW 2026-08-05** | evidence-based writeup (line-cited against `Main_Dyn.m`) of the 4 fatal bugs in the author's original solver and what `LSTE_solver_R1.m` fixed |
| `CODE_WRITING_GUIDE_EN/FA` (+ docx) | **NEW 2026-08-06** | teaches the codebase's recurring patterns (revision-file convention, solver structure, cfg mechanism, validation pattern, catalog-script pattern) |
| `governance/EXAMINER_REVIEW_FA` | **DONE** | strict examiner critique of Ch1–4 |
| `governance/IMPROVEMENT_SUGGESTIONS_FA` | **DONE** | 27-item priority list |
| `governance/DEFENSE_QA_FA` (+ docx) | **DONE** | 37 Q&A pairs; 5 formulas that rendered as raw `$...$` text (unsupported `\rm` LaTeX command) fixed 2026-08-06 |
| `governance/DEFENSE_JURY_REVIEW_R1` (+ docx) | **NEW 2026-08-29** | Mock defense-jury evaluation of the author's Google-Drive thesis draft ("پیش نویس پایان نامه (R 1.1).docx", modified 2026-08-29), covering the WHOLE document front-to-back (front matter, Ch1-5, references, appendices, language/typography) — not just the code-facing chapters. Produced from a text extraction of the .docx across 5 independent review passes plus direct verification (Persian typography counts, EndNote-field-leak count, reference-list audit). Explicit caveat stated up front: ~105 equations (MathType/OLE) and ~31 figures (embedded images) do not survive text extraction and were NOT assessed. Overall ≈74/100, "accept with moderate revisions". Top critical finding: Chapter 5 opens with the thesis template's own unedited instructional text (never replaced with the author's content); front matter has multiple unfilled placeholders and 3 conflicting dates; 47 raw EndNote XML field-code leaks in body text; Ch2 has no closing research-gap section despite Ch1 promising one; ref [8] is a near-identical prior MSc thesis at the same university, never differentiated; validation test 5 (the thesis's central Lord-Shulman-wave novelty) is the only qualitative one of five. Dated snapshot, not an evolving deliverable — a later re-review of a revised draft would be a new dated file, not an edit to this one. |
| `BACKGROUND_EN` | **DONE** | English mirror of the framing material |
| `LSTE_solver_R2016b_*` variants (`ch123_figs`, `catalog_conv`, `catalog_ch4_variants`) | **NEW 2026-08-06** | R2016b-compatible copies of the 3 figure scripts using post-2016b functions (`exportgraphics`→`print`, `yline`→`plot(xlim,...)`); originals untouched, verified identical output |

## 6. Records & governance (dated snapshots / living docs — tracked by git, not _R<n>)
| File | Role |
|---|---|
| `REVIEW_NOTES_final_author.docx` | round-1 supervisor review (authoritative) |
| `REVIEW2_PROM2.docx` | round-2 supervisor review (Prom.2) transcription |
| `PROJECT_AUDIT.md` | 2026-07-28 protocol/review/gap audit |
| `THESIS_TODO.md`, `WORK_PROTOCOL.md`, `DECISIONS_NEEDED.md`, `ROADMAP.md` | living governance (git history is the change log) |

---

## 7. Commit timeline (newest first — the dated progress trail)
| Commit | What |
|---|---|
| ecbf6f9 | **LSTE_solver_R9.m** — vectorized-assembly performance revision (§1): ~2.1× faster overall, ~2.5× on assembly; validated field-by-field vs R8 across 5 configs + the full 3000-step reference case (floating-point-roundoff match) plus an independent literature-benchmark re-check. R8 stays the thesis-of-record revision. |
| a08fd1e | `CODE_WRITING_GUIDE_EN/FA` (+ docx, §5) — bilingual guide to the codebase's recurring patterns |
| 7ac09c2 | `claude_*_2016b.m` (§5) — R2016b-compatible variants of 3 figure scripts (`exportgraphics`→`print`, `yline`→`plot(xlim,...)`), originals untouched |
| c8482f1 | Fixed 5 formulas in `DEFENSE_QA_FA.docx` rendering as raw `$...$` text (pandoc doesn't support the `\rm` LaTeX command) |
| 43967a7 | Ch3 Fig 3-3 re-plotted to show the porosity coefficient e=P_m² (was showing the mass factor P_m); added `CODE_FIX_HISTORY_EN/FA` (+ docx, §5) |
| 8d6fb26 | Project-wide plotting-rule sweep (§3 note) — no titles/floating text, solid color lines, across every active+archived figure script; found & fixed the `run()` cwd relative-path bug in 4 catalog scripts |
| 48c35bf | 13 editable Word versions of chapters/abstracts/TOC/figure-list/governance reports (§5) |
| 975d452 | Ch5 + both abstracts realigned to R4.4's real numbers after the `chapter_stats.csv` stale-folder fix; code_docs corrected to R8; 24 stale files archived (2026-08-05 code audit) |
| _(Prom.4b)_ | **Ch4 R4 — Prom.4 round 2 (Rezaei format + fixes):** figure captions rewritten in **Bahram Rezaei's thesis format** — bare figure number above (`۴-N`) + full caption below `شکل ۴-N: ‹توضیح› [‹پارامترها›]` with the case parameters in brackets (EN+FA). Fixes: bench1 legend `Present (LSTE_solver_R2)` → `Present`; **τ\*=0.87 removed** from the relaxation figure 4-14 and its text (EN+FA); **25-matrix top title removed**. Figures re-rendered; 4 docx rebuilt. |
| _(Prom.4)_ | **Ch4 R4 — Prom.4 figure/format corrections:** colour plots solid-only + `sgtitle` removed (4-panel); **academic figure numbering** (Figure 4-N above each figure, caption below) with in-text refs renumbered (EN+FA); **length symbol l → L** throughout; 25-matrix axis labels on every subplot; **convergence §4-3 restructured into 3 subparts (N_r/N_z/N_L) with the (c)/(d) panels replaced by tables** (2-panel `conv2` figures); jump/severe-change explanations added; verification diagrams kept. New render script `code/catalog/claude_catalog_conv2.m`; 4 docx rebuilt (24 figs each). See [[supervisor-prom4-review]] / `reviews/REVIEW4_PROM4.md`. |
| _(R4+tidy)_ | **Ch4 R4 finished + repo tidy:** figure **captions** (descriptive, FA+EN) on all four docx (Prom.1); **3 verification comparison figures** inlined in §4-4 (present vs Malekzadeh/ANSYS, exact Bessel, Bagri&Eslami — Prom.1 "diagrams not tables only"); `FIGURE_CAPTIONS_R4`; **folder tidy** (Prom.3 #1) — `.m`→`code/{solver,catalog,validation,extensions,misc}`, runners→`campaign/`, docs→`governance/`+`reviews/`; data/figures/thesis_chapter kept at root so relative paths still resolve (run scripts from `claude/` via `run('code/.../x.m')`). |
| _(Ch4 R4)_ | Chapter 4 R4 EN draft (53283b1) + FA mirror & 4 docx EN/FA×bw/color (5b021be) — see §4. |
| ecd8d24 | **Ch4 catalog R6 + campaign at new geometry (N_r=15):** 81-case campaign + N_z axial sweep + convergence/min-nodes study (mesh locked N_r=15/N_z=11); 4-panel + full-component + wave-front + 25-matrix + convergence figures; `FIGURE_CATALOG_R6_FA.docx` (44 figs); `run_ch4_campaign.ps1` + `run_nz_sweep.ps1`; raw solver `.mat` gitignored (>100 MB, regenerable) |
| caf505b | Catalog: observe the revision-number rule (R1 frozen, R2 = current) |
| 980e496 | Project audit: protocols + review-item status + gaps |
| 64806ea | Figure catalog: color + two-part split + smoother curves |
| 770d10b | Quantity catalog for choosing ch4 figures ("more graphs" feedback) |
| ce61453 | Chapter 5: conclusions and future work |
| 385eac2 | Code documentation (Prom.2 pages 3-4) |
| 47b8cc9 | Appendix A: Newmark method + comparison methods |
| aca4a20 | Chapter R3: referee number-check fixes |
| b68c337 | Results chapter R3 + Prom.2 review transcription |
| c0c132a | Chapter R3: 4 sections the workflow could not finish |
| 6d02617 | Governance files + complete revision figures (B&W + color) |
| 2109b01 | LSTE_solver_R6: per-end mechanical BC + cfg-overridable GPL dims |
| 99209bd | Revision runs (v4): pressure sweep, layers 9/15, weight-fill |
| 6235611 | Authoritative (author-corrected) chapter-4 review notes |
| 19b9ddf | Typed transcription of handwritten chapter-4 review notes |
| ec2e02a | Final results chapter R2 (FA+EN docx) |
| 82f38ac | Results chapter as Word documents (FA RTL + EN) |
| cf7650e | Results chapter: EN+FA text, B&W figures, captions, stats |
| 6858872 | ROADMAP.md: project path map |

*(full history: `git log --oneline` in the repo)*
